/**
 * This package contains configuration classes for Spring Boot,
 * including custom beans (OpenAPI dependency, for example),
 * security configurations, custom properties and integration with external APIs or services.
 */
package ${package}.config;
